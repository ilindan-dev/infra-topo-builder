// Package queries contains SQL source files used by sqlc to generate the
// type-safe Go code in internal/adapters/postgres/gen/. Keep SQL files here and
// regenerate the Go sources after changes with `sqlc generate`.
package queries
